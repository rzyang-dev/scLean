% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca.R
\name{RunPCA.Seurat}
\alias{RunPCA.Seurat}
\title{Run PCA (disk-backed IRLBA)}
\usage{
\\method{RunPCA}{Seurat}(object, npcs = 50, features = NULL, reduction.name = "pca", reduction.key = "PC_", tol = 1e-5, max.iter = 1000, ...)
}
\arguments{
\item{object}{A Seurat object}

\item{npcs}{Number of PCs to compute (default: 50)}

\item{features}{Features to use for PCA (default: VariableFeatures)}

\item{reduction.name}{Name of the reduction (default: "pca")}

\item{reduction.key}{Prefix for PC names (default: "PC_")}

\item{tol}{Convergence tolerance (default: 1e-5)}

\item{max.iter}{Maximum Lanczos iterations (default: 1000)}

\item{...}{Additional arguments passed to Seurat or scLean}
}
\value{
Seurat object with PCA reduction
}
\description{
Intercepts Seurat::RunPCA to route scLean-backed objects
to the disk-based implementation. Uses IRLBA (implicitly restarted
Lanczos bidiagonalization) with on-the-fly centering from HDF5.
}
\examples{
\donttest{
counts <- Matrix::rsparsematrix(200, 100, density = 0.15,
  dimnames = list(paste0("gene", 1:200), paste0("cell", 1:100)))
obj <- LoadScleanObject(counts)
obj <- NormalizeData(obj)
obj <- FindVariableFeatures(obj, nfeatures = 100)
obj <- ScaleData(obj)
obj <- RunPCA(obj, npcs = 10)
if (requireNamespace("Seurat", quietly = TRUE)) {
  Seurat::DimPlot(obj, reduction = "pca")
}
}
}
\seealso{
\code{\link{FindNeighbors.Seurat}}, \code{\link{FindClusters.Seurat}}
}
