% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize.R
\name{NormalizeData.scLeanAssay}
\alias{NormalizeData.scLeanAssay}
\title{Normalize data (disk-backed)}
\usage{
\method{NormalizeData}{scLeanAssay}(
  object,
  assay = NULL,
  normalization.method = "LogNormalize",
  scale.factor = 10000,
  margin = 2,
  chunk.size = NULL,
  in_memory = FALSE,
  ...
)
}
\arguments{
\item{object}{Seurat object containing scLeanAssay}

\item{assay}{Assay name (default: current default assay)}

\item{normalization.method}{"LogNormalize", "CLR", or "RC"}

\item{scale.factor}{Scale factor for LogNormalize (default: 10000)}

\item{margin}{1 for row, 2 for column (default: 2)}

\item{chunk.size}{Manual chunk size (auto if NULL)}

\item{in_memory}{If TRUE, return result in memory instead of HDF5}

\item{...}{Additional arguments}
}
\value{
Seurat object with normalized "data" layer on disk
}
\description{
S3 method for scLeanAssay. Performs chunked normalization directly
from the HDF5-backed count matrix, writing normalized values to the
"data" layer. Supports LogNormalize, CLR, and RelativeCounts (RC).
}
\examples{
\donttest{
counts <- Matrix::rsparsematrix(100, 50, density = 0.1,
  dimnames = list(paste0("gene", 1:100), paste0("cell", 1:50)))
obj <- LoadScleanObject(counts)
obj <- NormalizeData(obj)
obj <- NormalizeData(obj, normalization.method = "CLR")
}
}
\seealso{
\code{\link{ScaleData.scLeanAssay}},
  \code{\link{FindVariableFeatures.scLeanAssay}}
}
