% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale.R
\name{ScaleData.scLeanAssay}
\alias{ScaleData.scLeanAssay}
\title{Scale data (disk-backed)}
\usage{
\method{ScaleData}{scLeanAssay}(
  object,
  features = NULL,
  vars.to.regress = NULL,
  do.scale = TRUE,
  do.center = TRUE,
  chunk.size = NULL,
  in_memory = FALSE,
  ...
)
}
\arguments{
\item{object}{Seurat object with scLeanAssay}

\item{features}{Features to scale (default: all)}

\item{vars.to.regress}{Variables to regress out (not yet supported)}

\item{do.scale}{Whether to divide by standard deviation (default: TRUE)}

\item{do.center}{Whether to subtract mean (default: TRUE)}

\item{chunk.size}{Manual chunk size (auto if NULL)}

\item{in_memory}{Store in memory?}

\item{...}{Additional arguments}
}
\value{
Seurat object with scaled data (mean/sd stored in HDF5)
}
\description{
Computes per-gene mean and standard deviation for centering/scaling.
The scaled matrix is NOT materialized on disk — PCA applies
centering on-the-fly during Lanczos, keeping disk usage minimal.
Uses Welford's online algorithm for numerically stable variance
computation.
}
\examples{
\donttest{
counts <- Matrix::rsparsematrix(100, 50, density = 0.1,
  dimnames = list(paste0("gene", 1:100), paste0("cell", 1:50)))
obj <- LoadScleanObject(counts)
obj <- NormalizeData(obj)
obj <- FindVariableFeatures(obj, nfeatures = 50)
obj <- ScaleData(obj)
}
}
\seealso{
\code{\link{NormalizeData.scLeanAssay}},
  \code{\link{RunPCA.Seurat}}
}
